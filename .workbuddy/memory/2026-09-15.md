# 2026-09-15

## FGOA v1.00 → v1.01 升级完成

- 安装目录：`E:\Games\FGOA\FGOA\`（v1.00）；升级包：`E:\Games\FGOA\FGOA_v1.01\`（增量包，约 158.5 MiB / 53 个运行文件）。
- 升级方式：运行 `FGOA_v1.01\应用更新.cmd -InstallRoot "E:\Games\FGOA\FGOA"`，脚本会自动停止本地服务器、备份被替换文件、应用更新、改写 `App\fgo-launcher.json`（serverHost=auto、按需启动本地服务器、共享音频、关闭旧音频钩子）。
- 坑：用户最初解压的升级包有 21 个 0 字节文件（含 FGO_RunPowerShell.cmd、FGOLocalPlatform.exe、inject.exe、VC++ DLL），导致更新脚本静默跳过。原始压缩包 `FGOA_BAK\V1.01\FGOA前端_V1.01_Cloud23333.part1/part2.rar` **有解压密码**（用户已自行重新解压修复）。
- 升级成功（10:18），备份位于 `E:\Games\FGOA\FGOA\_update-backup\1.01-20260915-101803\`，`fgohook.pending.dll` 已清除。如需回退：停服后将备份覆盖回安装目录。
- 下一步：用户需用 `FGOLocalPlatform.exe` 启动，在"诊断与帮助"→"运行环境检查"后再启动游戏。

## FGOA v1.01 → v1.02 升级完成（17:13）

- 升级包：`E:\Games\FGOA\FGOA_V1.02\`（累计包，1050 个清单文件，含 1.01 全部内容）。本次解压完整，0 个零字节文件。
- 坑：首次运行更新时补丁文件已安装，但强化恢复步骤失败——`Server\venv\pyvenv.cfg` 的 `home = D:\py`（打包机残留路径），该 Python 在本机不存在。修复：将 `pyvenv.cfg` 的 home 改为安装目录自带的 `E:\Games\FGOA\FGOA\Server\python`（版本同为 3.10.11），venv 恢复正常后重新运行更新成功。
- 升级成功：备份 `FGOA\_update-backup\1.02-20260915-171309\`；强化恢复报告 `FGOA\logs\grail-recovery-1.02-20260915-171316.json` 显示 changed_profiles=0（无待修复强化数据，属正常）。
- 注意：若再次搬迁安装目录，pyvenv.cfg 的 home 需同步改到新路径。

## FGO汉化工具 使用方式确认（17:20）

- 工具：`E:\Games\FGOA\FGO汉化工具\FGO汉化工具.exe`（.NET 单文件 GUI，"FGO ARCADE · 汉化工作台"），三个标签页：纹理汉化 / 文本翻译 / 字体与加载。
- 使用流程：选择包含 rom 的游戏 App 文件夹（FGOA/FGOA_V1.01/FGOA_V1.02 下均有 App\rom 和 App\zh）→ 纹理页左右对照编辑 PNG 后"编码回包 → App\zh"；文本页 全量扫描 → 导出/导入翻译 JSON → 生成中文文本包。输出到 `App\zh` 外置覆盖，中文模式重启游戏生效，无需回包原始 farc。
- 坑：`FGO汉化工具\app-path.txt` 记录的旧路径 `E:\Games\FGOA\FGOA_Cloud23333\App` 已不存在，首次启动需重新选择 App 文件夹。

## 当前汉化覆盖包现状（17:45）

- `FGOA\App\zh`：80MB / 1450 个文件——文本类为主（single 1010、talk_list 281、talk_info 120、mst_data 23 个 farc），字体 SEGA_Skip-B.ttf，程序文本 executable-text.json (428K)。
- 纹理仅 7 个 sprite farc 已回包（spr_cmn_all、spr_exp_cmn、spr_gam_result、spr_sel_aimeinfo、spr_tuto_cmn/gam/sim）。
- 原始 `App\rom\sprite` 有 14360 个文件（farc + _table.bin 各半），绝大多数纹理未汉化；但其中大量是无文字的角色立绘/宝具图，真正需 PS 修改的只是含日文 UI 文字的少数。
- `FGOA_V1.01\App\zh` 和 `FGOA_V1.02\App\zh` 只有 fgozh.dll + rom，内容少，主汉化在 v1.00 的 FGOA 目录。

## 晚间重跑 v1.02 更新（20:30）

- 用户要求重新执行 v1.02 更新。发现 `Server\venv\pyvenv.cfg` 又被还原为 `home = D:\py`（文件时间戳为 Aug 21 原始打包时间，Server 目录在此期间被恢复/覆盖过），重新执行同样的 home 修复后更新成功。
- 新备份 `FGOA\_update-backup\1.02-20260915-203014\`，恢复报告 `logs\grail-recovery-1.02-20260915-203022.json`（changed_profiles=0，正常）。
- 教训：排查 venv 类故障时先 cat pyvenv.cfg 确认当前内容，不要假设之前的修复仍然生效。
